<div id="join">
	<div class="container">
		<div id="background">
			<% if @current_user %>
			<%= redirect_to "/diary/list" %>

			<% else %>
			<div id="sign_in_wrap">
				<div id="sign_in">
				<button target="/auth/facebook" id="sign_in" class="btn btn-info btn-block">페이스북 계정으로 시작</button>
				</div>
			</div>
			<% end %>
		</div>
	</div>
</div>
